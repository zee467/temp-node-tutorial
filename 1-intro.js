const amount = 12;

if (amount < 10) {
    console.log('small number');
}
else {
    console.log('large number');
}

console.log(`Hey it's my first node app!!!`);

// shortcut to access the terminal (ctrl + `)
// Run node app (node fileName.js). Do this in the terminal. N.B: The js extension after the filename is optional.