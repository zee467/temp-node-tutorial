const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is : ${num1 + num2}`);
}

addValues();

// This function is going to run in any module we import it. This is because the function is invoked.